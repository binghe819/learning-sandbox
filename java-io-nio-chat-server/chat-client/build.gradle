plugins {
    id 'java'
}

jar {
    manifest {
        attributes 'Version': version, 'Main-Class': 'com.binghe.io_client.IoChatClient'
    }
    from {
        configurations.compileClasspath.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
}
