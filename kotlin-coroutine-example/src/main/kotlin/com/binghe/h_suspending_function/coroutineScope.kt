package com.binghe.h_suspending_function

/**
 * 추가적인 suspend 함수들
 * - coroutineScope -> launch나 async처럼 새로운 코루틴을 만들지만, 주어진 함수 블록이 바로 실행되는 특징을 갖는다.
 *                     또한, 새로 생긴 코루틴과 자식 코루틴들이 모두 완료된 이후, 반환된다.
 *                     coroutineScope으로 만든 코루틴은 이전 코루틴의 자식 코루틴이 된다.
 * - 
 */
fun main() {

}